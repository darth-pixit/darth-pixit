import UIKit

// Phone window scene delegate — required once UIApplicationSceneManifest is declared.
// React Native manages the UIWindow itself; nothing extra is needed here.
class SceneDelegate: UIResponder, UIWindowSceneDelegate {
  var window: UIWindow?
}
